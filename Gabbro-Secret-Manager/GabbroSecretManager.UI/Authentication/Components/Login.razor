@attribute [RenderPage]
@attribute [BareLayout]

<div class="hero is-medium">
    <div class="hero-body">
        <div class="columns is-centered">
            <div class="column is-4">
                <p class="title is-2 has-text-centered">GSM</p>

                <div class="card">
                    <form class="card-content" hx-post="/authentication/login" hx-swap="none">

                        <div class="field">
                            <label class="label">Username</label>
                            <div class="control">
                                <input name="username" class="input" type="text" autocomplete="username" />
                            </div>
                        </div>
                        <div class="field">
                            <label class="label">Password</label>
                            <div class="control">
                                <input name="password" class="input" type="password" autocomplete="current-password" />
                            </div>
                            <p id="errors" class="help is-danger"></p>
                        </div>
                        <div class="field is-grouped">
                            <div class="control">
                                <button class="button is-primary">Sign in</button>
                                <a 
                                    href="/authentication/register" 
                                    hx-target="#page-container"
                                    hx-swap="innerHTML"
                                    hx-boost="true"
                                    class="button is-ghost">Register</a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

</div>